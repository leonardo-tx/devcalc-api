name: Dev Calc API Hello

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  hello@#:
    runs-on: ubuntu-latest

    steps:
      - name: Hello
        run: echo "Pipeline iniciado com sucesso"

